import SignalPathConsole from './SignalPathConsole.astro';

export default {
  title: 'Lab/Moises/Signal Path/Console',
  component: SignalPathConsole,
};

export const TransmissionMap = {
  parameters: {
    docs: {
      description: {
        story: 'A dense systems-console direction: the four signals are explicit input channels routed through a visible map into one output.',
      },
    },
  },
};
